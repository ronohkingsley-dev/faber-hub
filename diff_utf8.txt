diff --git a/src/api/client.js b/src/api/client.js
index fbd1375..d14ff65 100644
--- a/src/api/client.js
+++ b/src/api/client.js
@@ -2,9 +2,11 @@
 //  FABER.NET ┬╖ src/api/client.js
 // ΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉ
 import axios from 'axios';
+import { getBackendOriginalUrl as getBackendUrl } from '..\config.js';
+
 
 const API = axios.create({
-  baseURL: import.meta.env.VITE_API_URL || `${import.meta.env.VITE_BACKEND_URL || 'http://localhost:4000'}/api`,
+  baseURL: import.meta.env.VITE_API_URL || `${getBackendUrl()}/api`,
   timeout: 15000,
 });
 
diff --git a/src/components/CreateListing.jsx b/src/components/CreateListing.jsx
index f7199ff..840725b 100644
--- a/src/components/CreateListing.jsx
+++ b/src/components/CreateListing.jsx
@@ -7,6 +7,8 @@ import { X, Upload, Tag, Info, Video, CheckCircle, FileCode } from 'lucide-react
 import { useToast } from './Toast';
 import { useAuth } from '../context/AuthContext';
 import API from '../api/client';
+import { getBackendOriginalUrl as getBackendUrl } from '..\config.js';
+
 
 const CATEGORIES = [
   { value: 'drawing',     label: '≡ƒôÉ Drawing / DXF / DWG' },
@@ -37,7 +39,7 @@ export default function CreateListing({ onClose, onCreated }) {
     const formData = new FormData();
     formData.append('file', f);
     const { data } = await API.post(`/upload?type=${type}`, formData, { headers: { 'Content-Type': 'multipart/form-data' } });
-    return { url: (import.meta.env.VITE_BACKEND_URL || 'http://localhost:4000') + data.url };
+    return { url: getBackendUrl() + data.url };
   };
 
   const handleSubmit = async (e) => {
diff --git a/src/components/CreatePost.jsx b/src/components/CreatePost.jsx
index 1e039f0..161c12f 100644
--- a/src/components/CreatePost.jsx
+++ b/src/components/CreatePost.jsx
@@ -6,6 +6,8 @@ import { X, Upload, FileCode, Send } from 'lucide-react';
 import { useToast } from './Toast';
 import { useAuth } from '../context/AuthContext';
 import API from '../api/client';
+import { getBackendOriginalUrl as getBackendUrl } from '..\config.js';
+
 
 const POST_TYPES = [
   { value: 'blog',     label: '≡ƒô¥ BLOG / ANALYSIS' },
@@ -40,7 +42,7 @@ export default function CreatePost({ onClose, onCreated, postToEdit, onUpdated }
         const formData = new FormData();
         formData.append('file', file);
         const res = await API.post('/upload?type=post', formData, { headers:{'Content-Type':'multipart/form-data'} });
-        file_url = (import.meta.env.VITE_BACKEND_URL || 'http://localhost:4000') + res.data.url;
+        file_url = getBackendUrl() + res.data.url;
         file_name = file.name;
         file_type = file.name.split('.').pop();
       }
diff --git a/src/components/CreateReel.jsx b/src/components/CreateReel.jsx
index 672ec93..38c0e53 100644
--- a/src/components/CreateReel.jsx
+++ b/src/components/CreateReel.jsx
@@ -2,6 +2,8 @@ import { useState, useEffect } from 'react';
 import { X, Play, Upload, ShoppingBag } from 'lucide-react';
 import API from '../api/client';
 import { useToast } from './Toast';
+import { getBackendOriginalUrl as getBackendUrl } from '..\config.js';
+
 
 export default function CreateReel({ onClose, onCreated }) {
   const { push } = useToast();
@@ -30,7 +32,7 @@ export default function CreateReel({ onClose, onCreated }) {
       const { data } = await API.post('/reels', {
         title: form.title,
         description: form.description,
-        video_url: (import.meta.env.VITE_BACKEND_URL || 'http://localhost:4000') + uploadRes.data.url, // Full absolute path for static streaming
+        video_url: getBackendUrl() + uploadRes.data.url, // Full absolute path for static streaming
         listing_id: form.intent === 'market' ? form.listing_id : null,
       });
       push('Reel published successfully!', 'success');
diff --git a/src/components/Messenger.jsx b/src/components/Messenger.jsx
index 341ddd2..d4a21bc 100644
--- a/src/components/Messenger.jsx
+++ b/src/components/Messenger.jsx
@@ -7,6 +7,8 @@ import { useAuth } from '../context/AuthContext';
 import { io } from 'socket.io-client';
 import Peer from 'peerjs';
 import { useToast } from './Toast';
+import { getBackendOriginalUrl as getBackendUrl } from '..\config.js';
+
 
 export default function Messenger({ onClose, initialTarget = null }) {
   const { user } = useAuth();
@@ -30,7 +32,7 @@ export default function Messenger({ onClose, initialTarget = null }) {
 
   useEffect(() => {
     // 1. Establish Socket Connection
-    const s = io(import.meta.env.VITE_BACKEND_URL || 'http://localhost:4000');
+    const s = iogetBackendUrl();
     setSocket(s);
 
     s.on('connect', () => { s.emit('register', user.id); });
diff --git a/src/components/ModelViewer.jsx b/src/components/ModelViewer.jsx
index a6b974c..b7ce9f9 100644
--- a/src/components/ModelViewer.jsx
+++ b/src/components/ModelViewer.jsx
@@ -5,6 +5,8 @@
 // ΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉΓòÉ
 import { useRef, useState } from 'react';
 import { RotateCcw, ZoomIn, ZoomOut, Maximize2, Box } from 'lucide-react';
+import { getBackendOriginalUrl as getBackendUrl } from '..\config.js';
+
 
 const SUPPORTED_3D = new Set(['glb', 'gltf']);
 const SUPPORTED_IMG = new Set(['png', 'jpg', 'jpeg', 'webp', 'svg']);
@@ -79,7 +81,7 @@ export default function ModelViewer({ fileUrl, fileType, title, previewUrl }) {
             <p className="text-[10px] text-slate-600 mt-0.5">Download to view in your CAD application</p>
           </div>
           {fileUrl && (
-            <a href={`${import.meta.env.VITE_BACKEND_URL || 'http://localhost:4000'}${fileUrl}`} download
+            <a href={`${getBackendUrl()}${fileUrl}`} download
               className="text-[10px] font-black text-sky-400 bg-sky-500/10 border border-sky-500/20 px-3 py-1.5 rounded-lg hover:bg-sky-500/20 transition-all">
               DOWNLOAD FILE
             </a>
diff --git a/src/pages/MarketPage.jsx b/src/pages/MarketPage.jsx
index 5f49b72..3483e39 100644
--- a/src/pages/MarketPage.jsx
+++ b/src/pages/MarketPage.jsx
@@ -10,6 +10,8 @@ import ModelViewer from '../components/ModelViewer';
 import { useToast } from '../components/Toast';
 import { useAuth } from '../context/AuthContext';
 import API from '../api/client';
+import { getBackendOriginalUrl as getBackendUrl } from '..\config.js';
+
 
 const CATS = [
   { value: 'all',         label: 'ALL' },
@@ -155,7 +157,7 @@ function ListingDetail({ listing, onBack, onBuy, onPurchased, showPay, onClosePa
         {listing.demo_video_url && (
           <div className="mt-3">
             <p className="text-[9px] font-black text-slate-600 uppercase tracking-widest mb-1.5 flex items-center gap-1.5"><Video className="w-3 h-3" />DEMO VIDEO</p>
-            <video src={`${import.meta.env.VITE_BACKEND_URL || 'http://localhost:4000'}${listing.demo_video_url}`} controls className="w-full rounded-xl border border-white/5 max-h-64 object-contain bg-black" />
+            <video src={`${getBackendUrl()}${listing.demo_video_url}`} controls className="w-full rounded-xl border border-white/5 max-h-64 object-contain bg-black" />
           </div>
         )}
 
